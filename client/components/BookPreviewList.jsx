import React from 'react';
import List from './BookPreview';

export default class BookPreviewList extends React.Component {
  constructor(props){
    super(props);
  }
  render() {
    return (
      <div style={{ textAlign: 'center' }}>
      </div>
    );
  }
}